# Allow postinstall/build scripts for native and prisma packages.
# pnpm 11+ blocks these by default (ERR_PNPM_IGNORED_BUILDS).
# This file is always read — even with --frozen-lockfile in CI.
unsafe-perm=true
