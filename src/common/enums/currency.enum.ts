/** Virtual currencies (mirrors the Prisma `CurrencyType` enum). */
export enum CurrencyType {
  /** Purchased with real money; used for gifts/VIP/treasure/events. */
  GOLD = 'GOLD',
  /** Earned via activity; usable only in supported casual games. */
  FREE = 'FREE',
}
