// ============================================================
// Payments — recharge orders, IAP receipts, coin packages, gateways
// Owned by the "payments" module. Add models/enums here as the module
// is implemented. No cross-module relations by default — reference
// other domains by id and coordinate via the event bus.
// ============================================================
